# FastAPI and server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.17

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# Redis
redis==5.2.1

# Task Queue
celery[redis]==5.4.0
croniter>=2.0.0

# MinIO / S3
minio==7.2.12

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Rate Limiting
slowapi>=0.1.9

# Encryption
cryptography>=42.0.0

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Document Processing
pypdf==5.1.0
python-docx==1.1.2
python-magic==0.4.27
Pillow==10.4.0
piexif==1.1.3
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
pytesseract>=0.3.10
pdf2image>=1.16.3

# AI / Embeddings
httpx==0.28.1
numpy==2.2.1
duckduckgo-search>=7.0.0  # aka ddgs

# Web Browser (anti-bot patched)
patchright>=1.40.0
playwright-stealth>=2.0.0

# Web Crawling (LLM-friendly)
crawl4ai>=0.8.0
# scrapling installed separately in Dockerfile (lxml version conflict with crawl4ai)

# XML (WebDAV) — version resolved by crawl4ai/scrapling deps
# lxml pinned by scrapling (>=6.0.2) and crawl4ai (~=5.3, but works with 6.x)

# Utils
python-dotenv==1.0.1
